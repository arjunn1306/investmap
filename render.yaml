services:
  - type: web
    name: investmap
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: FLASK_ENV
        value: production
      - key: DATA_DIR
        value: /tmp
    autoDeploy: true
